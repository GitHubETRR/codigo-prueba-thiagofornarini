#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <stdbool.h>
#include <math.h>

#define N 10

int main(){
    int matriz[N][N];
    int primos[N*N];
    srand(time(NULL));
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            matriz[i][j] = rand() % 100 + 1; 
            printf("%2d ", matriz[i][j]);  
        }printf("\n");
    }
}

bool Detector (int matriz[i][j]){
    if (){

    }
}
